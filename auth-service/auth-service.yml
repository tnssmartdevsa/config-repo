spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hook_oauth
    username: hookup24
    password: hookup24
  jpa:
    hibernate:
        ddl-auto: update
    show-sql: true
    properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect 